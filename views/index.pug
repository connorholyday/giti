include ./includes/header.pug
    
.container
  h1 giti
  table(class='table table-striped table-hover table-condensed')
    thead
      tr
        th Name
        th Path
        th
        th
    tbody
      each row in results
        tr
          td=row.name
          td=row.path
          td: a(href='/commits/' + row.name) Commits
          td: a(href='/branches/' + row.name) Branches
        
include ./includes/footer.pug  