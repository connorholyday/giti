include ./includes/header.pug
    
.container
  h1 Repositories
  table(class='table table-striped table-hover table-condensed')
    thead
      tr
        th Name
        th Path
        th
    tbody
      each row in results
        tr
          td=row.name
          td=row.path
          td: a(href='/dash/' + row.name) See dashboard
        
include ./includes/footer.pug  